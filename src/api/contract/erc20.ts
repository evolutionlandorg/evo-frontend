// Copyright 2018-2021 evolution.land authors & contributors
// SPDX-License-Identifier: Apache-2.0

import { ethers } from 'ethers';
import { ethersApi } from '@evolutionland/evolution-js';
import { MaxUint256 } from '@ethersproject/constants';
import { LandId } from '@evolutionland/evolution-js/lib/config/constants';
// import type { ContractNameType } from '@evolutionland/evolution-js/types';

// export const _erc20Allowance = (landId: LandId, provider: ethers.providers.Provider, contractNameOrAddress, from: string, spender: string): Promise<string> => {
//   return ethersApi.erc20.erc20Allowance(landId, provider, contractNameOrAddress, from, spender);
// };

// export const _erc20Approve = (landId: LandId, signer: ethers.Signer, contractNameOrAddress, spender: string, value = MaxUint256.toHexString()): Promise<void> => {
//   return ethersApi.erc20.erc20Approve(landId, signer, contractNameOrAddress, spender, value);
// };
